apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.backend.name }}
  labels:
    run: {{ .Values.global.backend.name }}
spec:
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: {{ .Values.global.backend.name }}